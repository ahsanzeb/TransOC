


	subroutine memory( task, vtype, NElements, CallingRoutine )
	implicit none
	character(len=1), intent(in):: task
	character(len=*), intent(in):: vtype
	character(len=*), intent(in):: CallingRoutine
	integer, intent(in)                 :: NElements

	

	end subroutine memory

